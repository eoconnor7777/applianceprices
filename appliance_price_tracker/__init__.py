"""Reproducible Irish-retailer appliance price tracker.

Encodes the manual workflow (search each retailer for a specific model,
extract the live price, match it to the target variant, record it) so it
can be replayed on a schedule and analysed over time.
"""
__version__ = "0.1.0"
